const tileHeight = 16;
const tileWidth = 16;

const gameWidth = 800;
const gameHeight = 600;
const gameAspectRatio = gameWidth / gameHeight;


export const config = {
  tileWidth,
  tileHeight,
  gameWidth,
  gameHeight,
  gameAspectRatio,
  scale: 1,
  debug: false,
}

